<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
             x:Class="TestApp.views.DisplayExercises">
    <ListView x:Name="MyListView" ItemsSource="{Binding exercises}" BackgroundColor="White" SeparatorColor="black" HasUnevenRows="true">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Vertical">
                        <Label TextColor="Black" FontSize="Large" FontFamily="Arial"  HorizontalTextAlignment="Center" Text="{Binding exerciseName}"/>
                        <Button TextColor="Black" Text="View Details" BackgroundColor="DodgerBlue" Clicked="Details" CommandParameter="{Binding exerciseListKey}" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
