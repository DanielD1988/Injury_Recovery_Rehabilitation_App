<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
             xmlns:viewmodels="clr-namespace:TestApp.ViewModels"
             x:Class="TestApp.views.ListViewPage1">
    <ListView x:Name="MyListView" ItemsSource="{Binding exercises}" BackgroundColor="White" SeparatorColor="black" HasUnevenRows="true">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Vertical">
                        <Label Text="{Binding exerciseListKey}"/>
                        <Label Text="{Binding exerciseDescription}"/>
                        <Label Text="{Binding exerciseName}"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
